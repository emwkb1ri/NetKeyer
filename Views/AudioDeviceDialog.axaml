<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:NetKeyer.ViewModels"
        x:Class="NetKeyer.Views.AudioDeviceDialog"
        x:DataType="vm:AudioDeviceDialogViewModel"
        Title="NetKeyer+Remote - Audio Output Device Selection"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        CanResize="false">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="AudioDialogBorderBrush" Color="#C8D2DF"/>
                    <SolidColorBrush x:Key="AudioDialogSecondaryTextBrush" Color="#4A5A70"/>
                    <SolidColorBrush x:Key="AudioDialogTertiaryTextBrush" Color="#5F7088"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="AudioDialogBorderBrush" Color="#4A5870"/>
                    <SolidColorBrush x:Key="AudioDialogSecondaryTextBrush" Color="#AAB8CF"/>
                    <SolidColorBrush x:Key="AudioDialogTertiaryTextBrush" Color="#C2CEE2"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="15" RowDefinitions="Auto,*,Auto">

        <!-- Title and Instructions -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,15">
            <TextBlock Text="Audio Output Device Selection"
                       FontSize="18"
                       FontWeight="Bold"/>
            <TextBlock Text="Select the audio output device for sidetone. Changes take effect immediately."
                       FontSize="12"
                       Foreground="{DynamicResource AudioDialogSecondaryTextBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Device Selection -->
        <Border Grid.Row="1"
            BorderBrush="{DynamicResource AudioDialogBorderBrush}"
                BorderThickness="1"
                Padding="12"
                CornerRadius="3">
            <StackPanel Spacing="10">
                <TextBlock Text="Audio Output Device:"
                           FontWeight="SemiBold"
                           FontSize="13"/>

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <ComboBox ItemsSource="{Binding AudioDevices}"
                              SelectedItem="{Binding SelectedAudioDevice}"
                              Width="320"
                              PlaceholderText="Select an audio device..."/>
                    <Button Content="Refresh"
                            Command="{Binding RefreshAudioDevicesCommand}"
                            Width="80"
                            HorizontalContentAlignment="Center"
                            Padding="6"/>
                </StackPanel>

                <CheckBox Content="Aggressive Low-Latency Mode (Windows only)"
                          IsChecked="{Binding AggressiveLowLatency}"
                          IsVisible="{Binding IsWindowsOnly}"
                          FontSize="11"
                          Foreground="{DynamicResource AudioDialogSecondaryTextBrush}"
                          Margin="0,10,0,0"/>
                <TextBlock Text="When enabled: Lowest latency, may cause audio artifacts"
                           FontSize="10"
                           Foreground="{DynamicResource AudioDialogTertiaryTextBrush}"
                           TextWrapping="Wrap"
                           Margin="20,3,0,0"
                           IsVisible="{Binding IsWindowsOnly}"/>
                <TextBlock Text="When disabled: Higher latency, most compatible"
                           FontSize="10"
                           Foreground="{DynamicResource AudioDialogTertiaryTextBrush}"
                           TextWrapping="Wrap"
                           Margin="20,2,0,0"
                           IsVisible="{Binding IsWindowsOnly}"/>

                <CheckBox Content="Keep audio device awake"
                          IsChecked="{Binding KeepAudioDeviceAwake}"
                          FontSize="11"
                          Foreground="{DynamicResource AudioDialogSecondaryTextBrush}"
                          Margin="0,10,0,0"/>

                <CheckBox Content="Enable detailed timing analysis logging"
                          IsChecked="{Binding EnableDetailedTimingAnalysisLogging}"
                          FontSize="11"
                          Foreground="{DynamicResource AudioDialogSecondaryTextBrush}"
                          Margin="0,8,0,0"/>
                <TextBlock Text="Disabled by default. Enable only while collecting latency diagnostics."
                           FontSize="10"
                           Foreground="{DynamicResource AudioDialogTertiaryTextBrush}"
                           TextWrapping="Wrap"
                           Margin="20,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="10"
                    Margin="0,15,0,0">
            <Button Name="CancelButton"
                    Content="Cancel"
                    Width="80"
                    HorizontalContentAlignment="Center"
                    Padding="6"
                    Click="CancelButton_Click"/>
            <Button Name="OkButton"
                    Content="OK"
                    Width="80"
                    HorizontalContentAlignment="Center"
                    Padding="6"
                    Click="OkButton_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
