<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:NetKeyer.ViewModels"
        x:Class="NetKeyer.Views.MidiConfigDialog"
        x:DataType="vm:MidiConfigDialogViewModel"
        Title="NetKeyer+Remote - MIDI Note Configuration"
        SizeToContent="WidthAndHeight"
        MaxHeight="760"
        WindowStartupLocation="CenterOwner"
        CanResize="true">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="MidiDialogBorderBrush" Color="#C8D2DF"/>
                    <SolidColorBrush x:Key="MidiDialogHeaderBackgroundBrush" Color="#E7ECF3"/>
                    <SolidColorBrush x:Key="MidiDialogHeaderForegroundBrush" Color="#1A2433"/>
                    <SolidColorBrush x:Key="MidiDialogSectionBackgroundBrush" Color="#F7F9FC"/>
                    <SolidColorBrush x:Key="MidiDialogSectionForegroundBrush" Color="#243246"/>
                    <SolidColorBrush x:Key="MidiDialogSecondaryTextBrush" Color="#4A5A70"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="MidiDialogBorderBrush" Color="#4A5870"/>
                    <SolidColorBrush x:Key="MidiDialogHeaderBackgroundBrush" Color="#2A3444"/>
                    <SolidColorBrush x:Key="MidiDialogHeaderForegroundBrush" Color="#EAF1FF"/>
                    <SolidColorBrush x:Key="MidiDialogSectionBackgroundBrush" Color="#1F2633"/>
                    <SolidColorBrush x:Key="MidiDialogSectionForegroundBrush" Color="#D9E4F7"/>
                    <SolidColorBrush x:Key="MidiDialogSecondaryTextBrush" Color="#AAB8CF"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="15" RowDefinitions="Auto,Auto,*,Auto,Auto">

        <!-- Title and Instructions -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,15">
            <TextBlock Text="MIDI Note Configuration"
                       FontSize="18"
                       FontWeight="Bold"/>
            <TextBlock Text="Assign MIDI notes (0-127) to one or more functions. Multiple functions can be assigned to the same note."
                       FontSize="12"
                       Foreground="{DynamicResource MidiDialogSecondaryTextBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Add Note Section -->
        <Border Grid.Row="1"
            BorderBrush="{DynamicResource MidiDialogBorderBrush}"
                BorderThickness="1"
                Padding="10"
                CornerRadius="3"
                Margin="0,0,0,10">
            <StackPanel Spacing="6">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBlock Text="Add Note:"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"/>
                    <NumericUpDown Value="{Binding SelectedNoteNumber}"
                                   Minimum="0"
                                   Maximum="127"
                                   Width="120"
                                   ShowButtonSpinner="True"
                                   FormatString="0"
                                   AllowSpin="True"
                                   ParsingNumberStyle="Integer"/>
                    <Button Content="Add"
                            Command="{Binding AddNoteCommand}"
                            Width="60"
                            HorizontalContentAlignment="Center"
                            Padding="5"/>
                    <TextBlock Text="(MIDI note 0-127)"
                               VerticalAlignment="Center"
                               FontSize="11"
                               Foreground="{DynamicResource MidiDialogSecondaryTextBrush}"/>
                </StackPanel>
                <TextBlock Text="{Binding AddNoteError}"
                           Foreground="Red"
                           FontSize="11"
                           IsVisible="{Binding AddNoteError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                           Margin="0,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Note Configurations Table -->
        <Border Grid.Row="2"
            BorderBrush="{DynamicResource MidiDialogBorderBrush}"
                BorderThickness="1"
                CornerRadius="3">
            <ScrollViewer>
                <StackPanel>
                    <!-- Header Row -->
                        <Border BorderBrush="{DynamicResource MidiDialogBorderBrush}"
                            BorderThickness="0,0,0,1"
                            Background="{DynamicResource MidiDialogHeaderBackgroundBrush}"
                            Padding="8,6">
                        <Grid ColumnDefinitions="70,90,90,90,90,60">
                            <TextBlock Grid.Column="0" HorizontalAlignment="Center" Text="Note #" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource MidiDialogHeaderForegroundBrush}"/>
                            <TextBlock Grid.Column="1" HorizontalAlignment="Center" Text="Left Paddle" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource MidiDialogHeaderForegroundBrush}"/>
                            <TextBlock Grid.Column="2" HorizontalAlignment="Center" Text="Right Paddle" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource MidiDialogHeaderForegroundBrush}"/>
                            <TextBlock Grid.Column="3" HorizontalAlignment="Center" Text="Straight Key" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource MidiDialogHeaderForegroundBrush}"/>
                            <TextBlock Grid.Column="4" HorizontalAlignment="Center" Text="PTT" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource MidiDialogHeaderForegroundBrush}"/>
                            <TextBlock Grid.Column="5" HorizontalAlignment="Center" Text="Remove" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource MidiDialogHeaderForegroundBrush}"/>
                        </Grid>
                    </Border>

                    <!-- Data Rows -->
                    <ItemsControl ItemsSource="{Binding NoteConfigurations}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="vm:MidiNoteConfigRow">
                                <Border BorderBrush="{DynamicResource MidiDialogBorderBrush}"
                                        BorderThickness="0,0,0,1"
                                        Padding="8,6">
                                    <Grid ColumnDefinitions="70,90,90,90,90,60">
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding NoteNumber}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontFamily="Consolas,Courier New,monospace"/>
                                        <CheckBox Grid.Column="1"
                                                  IsChecked="{Binding IsLeftPaddle}"
                                                  HorizontalAlignment="Center"/>
                                        <CheckBox Grid.Column="2"
                                                  IsChecked="{Binding IsRightPaddle}"
                                                  HorizontalAlignment="Center"/>
                                        <CheckBox Grid.Column="3"
                                                  IsChecked="{Binding IsStraightKey}"
                                                  HorizontalAlignment="Center"/>
                                        <CheckBox Grid.Column="4"
                                                  IsChecked="{Binding IsPtt}"
                                                  HorizontalAlignment="Center"/>
                                        <Button Grid.Column="5"
                                                Content="×"
                                                Command="{Binding $parent[Window].DataContext.RemoveNoteCommand}"
                                                CommandParameter="{Binding}"
                                                FontSize="16"
                                                Width="30"
                                                Height="25"
                                                Padding="0"
                                                HorizontalAlignment="Center"
                                                HorizontalContentAlignment="Center"
                                                VerticalContentAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Function Descriptions -->
        <Border Grid.Row="3"
                BorderBrush="{DynamicResource MidiDialogBorderBrush}"
                BorderThickness="1"
                Background="{DynamicResource MidiDialogSectionBackgroundBrush}"
                Padding="10"
                CornerRadius="3"
                Margin="0,10,0,0">
            <StackPanel Spacing="4">
                <TextBlock FontWeight="SemiBold" FontSize="11" Margin="0,0,0,3" Foreground="{DynamicResource MidiDialogSectionForegroundBrush}">Function Descriptions:</TextBlock>
                <TextBlock FontSize="10" Foreground="{DynamicResource MidiDialogSectionForegroundBrush}">• Left Paddle: Used as dit paddle in iambic mode</TextBlock>
                <TextBlock FontSize="10" Foreground="{DynamicResource MidiDialogSectionForegroundBrush}">• Right Paddle: Used as dah paddle in iambic mode</TextBlock>
                <TextBlock FontSize="10" Foreground="{DynamicResource MidiDialogSectionForegroundBrush}">• Straight Key: Key down/up in straight key mode</TextBlock>
                <TextBlock FontSize="10" Foreground="{DynamicResource MidiDialogSectionForegroundBrush}">• PTT: Push-to-talk for non-CW modes</TextBlock>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="10"
                    Margin="0,15,0,0">
            <Button Content="Reset to Defaults"
                    Command="{Binding ResetToDefaultsCommand}"
                    Width="130"
                    HorizontalContentAlignment="Center"
                    Padding="6"/>
            <Button Name="CancelButton"
                    Content="Cancel"
                    Width="80"
                    HorizontalContentAlignment="Center"
                    Padding="6"
                    Click="CancelButton_Click"/>
            <Button Name="OkButton"
                    Content="OK"
                    Width="80"
                    HorizontalContentAlignment="Center"
                    Padding="6"
                    Click="OkButton_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
